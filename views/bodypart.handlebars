<section class="container">
    <h3 class="gallery-name">{{bodypart.name}}</h3>
    <section class="flex-row justify-center">
        {{#each bodypart.workouts as |workout|}}
        {{> workout-details}}
        {{/each}}
    </section>
</section>